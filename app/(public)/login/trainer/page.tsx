import { LoginPage } from "@/components/public/login-page";

export default function TrainerLogin() {
  return (
    <LoginPage
      title="Trainer Login"
      description="Sign in to manage your assigned batches, attendance, and assignments."
    />
  );
}
