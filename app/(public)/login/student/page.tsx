import { LoginPage } from "@/components/public/login-page";

export default function StudentLogin() {
  return (
    <LoginPage
      title="Student Login"
      description="Sign in to view your programs, attendance, payments, and certificates."
    />
  );
}
